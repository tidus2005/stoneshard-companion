# 0.3.6 点击循环变速与边缘再次按键切图

速度按钮由下拉菜单改为直接循环 1×、2×、3×，使用一枚、两枚、三枚箭头图标区分正常、加速、急速。图标与文字随所选倍率更新，继续保留切图和重连时的倍率意图。旧版 4× 快捷键保持兼容，从 4× 点击按钮回到 1×。

普通方向键先沿人物所在行或列走到内侧边缘后停止；新的独立按下在对应方向内侧一格或出口格时，沿当前位置点击最外侧格一次，进入下一张地图后结束。九宫格四向在边缘也采用同样目标，不把人物先拉回中点。四角和中心功能保持原行为。

处理“人物已经到达，但监督定时器还未记录到达”的短暂间隔：只有同方向、同人物、同场景、已到原目标并且安全空闲时才接受出口请求；途中按键仍停止原路线。按住不放产生的重复键不会触发出口或恢复路线。两格以外仍先靠近边缘，反方向按键正常向地图内部行走。敌人、受伤、面板、场景改变及超时检查继续生效。

桥接映射升级到 `Local\StoneshardCompanion.v7.<pid>`，共享数据仍为 3968 字节，旧 v1–v6 映射会被拒绝混用。`walk_phase=1` 同时表示九宫格出口步骤和边缘主动出口请求。只调用原版移动和停止脚本，不强制切房间或攻击。

运行 `scripts/test-offline.ps1`：27 项原生补给、164 项原生交互、39 项速度意图、95 项托管检查，以及 8 项存档核心回归通过，共 333 项；构建无警告、无错误。新增检查覆盖三档循环、图标/文案、八种方向入口在边缘的目标、到达定时竞争、重复键抑制、单次切图、到达外圈、反向行走，以及出口前遇敌/受伤/面板/场景变化。

本轮不启动游戏，不控制桌面，不修改真实存档；离线通过不代表实际游戏场景已验收。GitHub Windows 构建服务器还会执行便携包完整解压和包内 PowerShell 存档测试，全部通过后发布 ZIP。
