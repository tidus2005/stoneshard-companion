Windows 11 x64 免安装版。下载 `StoneshardCompanion-0.3.9-win-x64.zip`，完整解压到新目录后双击 **Start.cmd**。

**本次桥接更新到 v8：请先在游戏内保存并正常退出游戏，再退出旧助手，启动新版与游戏。只重启助手不会替换已加载的旧桥接。**

## 本次修复

- **边缘再次按方向键切图**：按游戏实际生成的出口格和方向处理，已站在出口上时调用原版出口流程，不再重复发起零距离寻路；遇敌、受伤或手动接管仍停止。
- **中心被占自动寻找空位**：先检查单位占用与路径，按距离选择中心周围最多 8 格范围内的可达空位；没有可达位置时停止，不攻击占位单位。
- **虚拟机备份路径兼容**：解析重定向“文档”等配置根目录，区分虚拟存储标记和真正的路径链接；修复一律拒绝 `ReparsePoint` 的问题。保留存档内部链接拒绝、校验、保险备份及传输失败本地副本保护。
- 存档管理显示版本，增加“复制状态与路径”，错误包含失败步骤与错误码。

400 项离线检查通过，并验证实际解压程序的备份/还原、真实目录链接、特殊字符路径、无 PowerShell 环境及 Windows SMB 环回共享。测试只使用临时存档。

**本版未进行游戏内或真实 Mac 虚拟机实测。** 云/虚拟存储标签为策略测试；此次修复不等于所有共享驱动均已验证。完整说明见仓库 `docs/0.3.9走图与重定向目录修复.md`。

继续随包提供 .NET，无需 PowerShell，支持原有 ZIP 备份。适配 Steam 原版 Stoneshard 0.9.4.25 / Build 24780451；Windows x64 包不是 macOS 或 Windows ARM64 原生应用。
