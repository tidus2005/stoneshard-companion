Windows 11 x64（Intel / AMD）免安装便携版。

1. 下载下方 **StoneshardCompanion-0.3.5-win-x64.zip**，不要下载 Source code。
2. 完整解压，保留 EXE、DLL、Assets、Runtime。
3. 双击 **Start.cmd** 或 **StoneshardCompanion.exe**。

**已自带 .NET 和 PowerShell，存档备份与还原也无需另装依赖。** 包内不含游戏、个人存档或本机设置。升级前正常退出旧助手和游戏。

包含九宫格导航、方向键自动移动、1–4 倍速度与切图记忆、物品常显、面甲、角色状态、镜头控制、自动补给、图形存档备份及还原。

适配 **Steam 原版 Stoneshard 0.9.4.25 / Build 24780451**；本包为 x64，非 ARM64 原生版。

通过离线回归和真实 ZIP 解压测试，确认存档服务使用包内 PowerShell。未在另一台 Windows 11 电脑或游戏内实测，既有游戏功能的实测范围见仓库文档。

附带 `.sha256` 下载校验文件，包内另有逐文件 `SHA256.json`。
