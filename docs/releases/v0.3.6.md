Windows 11 x64（Intel / AMD）免安装便携版。完整解压 ZIP，双击 **Start.cmd** 或 **StoneshardCompanion.exe**；已自带 .NET 和 PowerShell，备份与还原无需另装依赖。

## 本次改动

- **点击速度图标直接切换**：正常 1× → 加速 2× → 急速 3× → 正常。三个不同图标显示当前档位，不再弹出容易被遮挡的下拉菜单。
- **边缘再次按同方向切图**：方向键模式先走到边缘，松开再按同方向会点击人物正前方出口，进入相邻地图后结束。已手动走到出口附近也可直接触发；九宫格四向按钮同样按当前位置处理出口。
- 保持原版寻路、回合与物品消耗；长按不连续跨图，遇敌、受伤、开面板、失联或手动接管时结束路线，出口不可达时不重复点击。

**升级前正常退出旧助手和游戏，再启动新版**：本版连接组件升级为 v7，旧组件仍在游戏中时会提示重新启动。

适配 Steam 原版 **Stoneshard 0.9.4.25 / Build 24780451**；x64，非 ARM64 原生版。下载 `StoneshardCompanion-0.3.6-win-x64.zip`，不要下载 Source code。保留包内全部文件与文件夹。

本地 333 项离线检查通过；发行流程另在 Windows 构建服务器执行回归与便携包测试。未启动游戏实测，本次边缘点击行为仍需游戏内验收。附 ZIP 的 `.sha256` 和包内逐文件 `SHA256.json`。
